import { createStyles } from "antd-style";

export const useHomePageStyles = createStyles(({ css }) => ({
  backGroundsBlur: css``,
  container: css`
    svg {
      position: absolute;
      top: 0;
      left: 0;
      width: 120dvw;
      height: 110dvh;
    }
  `
}));
