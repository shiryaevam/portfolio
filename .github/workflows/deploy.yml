name: Deploy Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '22'

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'
          cache-dependency-path: yarn.lock

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run TypeScript check
        run: yarn type-check

      - name: Run ESLint
        run: yarn lint

  build:
    runs-on: ubuntu-latest
    needs: test
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'
          cache-dependency-path: yarn.lock

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build project
        run: yarn build

      - name: Upload build artifacts
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: dist/
          retention-days: 2

  deploy-production:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    environment: production
    timeout-minutes: 20
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-output
          path: dist

      - name: Prepare remote directory
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          script: |
            mkdir -p ~/portfolio/dist

      - name: Upload dist to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          source: "dist/**"
          target: "~/portfolio/dist"
          strip_components: 1
          overwrite: true

      - name: Deploy to Production Server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          script: |
            set -euo pipefail
            echo "🚀 Начинаем деплой..."
            echo "📅 Время: $(date)"
            echo "👤 Пользователь: $(whoami)"
            echo "📁 Домашняя папка: $(pwd)"
            
            # Шаг 1: Проверяем текущее состояние
            echo "📋 Шаг 1: Проверяем состояние сервера"
            echo "💾 Свободное место:"
            df -h ~
            echo "📂 Содержимое домашней папки:"
            ls -la ~
            
            # Шаг 2: Переходим в папку проекта
            echo "📋 Шаг 2: Переходим в папку проекта"
            if [ -d "$HOME/portfolio" ]; then
              echo "✅ Папка ~/portfolio существует"
              cd ~/portfolio
              echo "📁 Текущая папка: $(pwd)"
              echo "📂 Содержимое папки portfolio:"
              ls -la
              
              # Если это не git репозиторий, но папка не пустая
              if [ ! -d ".git" ] && [ "$(ls -A . 2>/dev/null)" ]; then
                echo "🧹 Папка не пустая, но не является git репозиторием"
                echo "📦 Сохраняем существующие файлы..."
                mkdir -p ../portfolio-backup-$(date +%Y%m%d-%H%M%S)
                mv * ../portfolio-backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true
                mv .* ../portfolio-backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true
                echo "📥 Клонируем репозиторий..."
                git clone https://github.com/shiryaevam/portfolio.git .
              fi
            else
              echo "❌ Папка ~/portfolio не найдена!"
              echo "📦 Создаем папку и клонируем репозиторий..."
              mkdir -p ~/portfolio
              cd ~/portfolio
              git clone https://github.com/shiryaevam/portfolio.git .
              echo "✅ Репозиторий склонирован"
            fi
            
            # Шаг 3: Проверяем Git репозиторий
            echo "📋 Шаг 3: Проверяем Git репозиторий"
            if [ -d ".git" ]; then
              echo "✅ Git репозиторий найден"
              echo "🔗 Текущий remote:"
              git remote -v
              echo "🌿 Текущая ветка:"
              git branch
              echo "📝 Последний коммит:"
              git log -1 --oneline
            else
              echo "❌ Git репозиторий не найден!"
              exit 1
            fi
            
            # Шаг 4: Проверяем docker-compose.yml
            echo "📋 Шаг 4: Проверяем docker-compose.yml"
            if [ -f "docker-compose.yml" ]; then
              echo "✅ docker-compose.yml найден"
            else
              echo "❌ docker-compose.yml не найден!"
              echo "📂 Файлы в текущей папке:"
              ls -la
              exit 1
            fi
            
            # Шаг 5: Останавливаем контейнеры
            echo "📋 Шаг 5: Останавливаем контейнеры"
            if docker compose -p portfolio ps | grep -q "Up"; then
              echo "🛑 Останавливаем запущенные контейнеры..."
              docker compose -p portfolio down --remove-orphans
              echo "✅ Контейнеры остановлены"
            else
              echo "ℹ️ Контейнеры не запущены"
            fi
            
            # Шаг 6: Обновляем код
            echo "📋 Шаг 6: Обновляем код"
            echo "🔄 Настраиваем remote на HTTPS..."
            git remote set-url origin https://github.com/shiryaevam/portfolio.git
            echo "📥 Получаем последние изменения..."
            git fetch origin main
            echo "🔄 Обновляем код..."
            git reset --hard origin/main
            git pull origin main
            echo "✅ Код обновлен"
            
            # Шаг 7: Запускаем контейнеры (без сборки)
            echo "📋 Шаг 7: Запускаем контейнеры"
            echo "▶️  Запускаем контейнеры без сборки..."
            docker compose -p portfolio up -d
            echo "✅ Контейнеры запущены"
            
            # Шаг 8: Проверяем результат
            echo "📋 Шаг 8: Проверяем результат"
            echo "⏳ Ждем 15 секунд для запуска..."
            sleep 15
            echo "📊 Статус контейнеров:"
            docker compose -p portfolio ps
            echo "🎉 Деплой завершен!"
