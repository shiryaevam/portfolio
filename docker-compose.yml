version: '3.8'
services:
  web:
    build: .
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge